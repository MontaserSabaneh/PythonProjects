# Inventory Classification

A Python project for reading objects from a JSON file and classifying them by weight.

## Run

```bash
python3 main.py --file_path data.json
```

## Test

```bash
python3 -m unittest discover tests
```